<%- layout('layouts/boilerplate') %>
<link rel="stylesheet" href="/css/stars.css">

<div class="row d-flex flex-lg-row">
    <div class="col-12 col-lg-8">
        <% if(currentUser && club.author.equals(currentUser._id)){ %> 
          <h2>Add a post</h2>
              <form action="/institutes/<%- club.institute %>/<%- club._id %>/posts" method="POST" class="mb-3 validated-form" novalidate>
                <div class="mb-3">
                  <label class="form-label" for="post-title">Title</label><br>
                  <input class="form-control" type="text" name="post[title]" id="post-title">
                </div>
                <div class="mb-3">
                  <label class="form-label" for="post-desc">Description</label><br>
                  <textarea class="form-control" name="post[description]" id="body" cols="30" rows="3" required></textarea>
                </div>
                <button class="btn btn-success">Submit</button>
              </form>
        <% } %> 
      </div>
      <div class="col-12 col-lg-4">
        <div class="card mb-3">
            <div class="card-body">
              <h5 class="card-title"><%= club.clubName %></h5>
              <p class="card-text"><%= club.description %></p>
            </div>
            <ul class="list-group list-group-flush">
              <li class="list-group-item text-muted"><%= club.institute %></li>
            </ul>
        </div>
    </div>
    
</div>
